Algoritmo Operadoreslógicos
	
	// (y): Si los dos son verdaderos devuelve verdadero. AND &&
	//resultado = 20 > 10 y 50 < 100
	//mostrar resultado
	
	// (o): Basta con que uno sea verdadero para que devuelva verdadero. OR ||
	resultado1 = 20 > 10 | 50 > 100
	mostrar resultado1
	
	//! (no): Negación
	
	resultado3 = !Verdadero
	mostrar resultado3
	
	
FinAlgoritmo
